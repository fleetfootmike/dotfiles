# Dotfiles

Personal dotfiles setup.

There are no secrets in here, so don't bother looking :D

Run `./install.sh` to deploy these into your home directory and check
prerequisites (`./install.sh --help` for options).
